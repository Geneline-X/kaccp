"use client"
export default function AvatarUpload(_props: { onUploaded: (url: string) => void }) {
  // Temporarily disabled: upload button removed while UploadThing is being configured.
  // Returning null keeps the layout clean and prevents broken interactions.
  return null
}
